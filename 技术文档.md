# 大学生双创赛事管理系统 - 技术架构实现文档

## 1. 项目概述

本项目是一个基于 Next.js 的全栈应用，旨在提供高校创新创业赛事的发布、报名、审批及成就管理的业务闭环。系统采用前后端同构架构，依赖 Server Actions 处理核心业务逻辑，通过 Prisma 进行数据持久化，并集成了无头浏览器端 PDF 生成方案。

### 技术栈
* **前端框架:** Next.js (App Router), React 18
* **UI 框架:** Tailwind CSS, shadcn/ui
* **持久层:** PostgreSQL / MySQL, Prisma ORM
* **身份认证:** Clerk
* **核心依赖:** `html2canvas`, `jspdf`

---

## 2. 核心架构设计

### 2.1 路由与权限控制
* **实现方案:** 采用 Clerk 进行 JWT 鉴权。在服务端获取 Session Claims，解析用户角色 (`role`)。
* **路由分流:** 在应用根路由 (`/`) 顶层组件中，基于用户角色执行服务端重定向 (`redirect`)。管理员自动路由至 `/admin`，普通用户路由至 `/competitions`，未登录用户渲染静态门户页，减少无意义的客户端组件渲染与网络状态轮询。

### 2.2 数据库设计与查询优化
* **索引建立:** 针对业务逻辑中存在高频查询和连表查询的字段（如 `status`, `userId`, `competitionId`），在 Prisma schema 中配置了底层数据库索引（B+ 树），以保证表数据量级增长时的查询效率。
* **模型拓展:** 赛事模型 (`Competition`) 引入 `level` 字段（如校级、省级、国家级），为后续数据统计和履历生成提供必要的维度区分。

---

## 3. 核心业务逻辑实现

### 3.1 报名与成就的数据流解耦
* **问题背景:** 早期设计中，赛事报名与荣誉获取强耦合，导致数据不符合实际业务逻辑。
* **改造方案:** 将业务流拆分为独立的“参赛流程”与“成就审核流程”。
    1.  **报名阶段:** 仅创建 `Registration` 记录，由管理员审批控制参赛资格。
    2.  **成就获取:** 通过 `Achievement` 模型独立管理，并引入状态机 (`status`: PENDING, APPROVED, REJECTED)。
    3.  **数据过滤:** 个人看板与导出引擎仅查询并展示 `status === 'APPROVED'` 的成就数据，保证履历数据的真实性。

### 3.2 基于事务的数据一致性保障
* **场景:** 用户取消赛事报名时的名额释放问题。
* **实现方案:** 在 Server Action 中使用 Prisma 的 `$transaction` 机制，将“删除报名记录”与“赛事 `currentParticipants` 字段递减 (`decrement: 1`)”封装为原子操作。确保在并发或异常情况下，名额数据的变更与报名状态强一致，避免出现脏数据或死锁。

---

## 4. 前端工程化与交互优化

### 4.1 复杂渲染组件的按需加载 (Code Splitting)
* **性能瓶颈:** PDF 生成依赖 `html2canvas` 和 `jsPDF`，这两个库体积较大，若静态引入会显著增加页面初始加载包大小 (Bundle Size)。
* **优化方案:** 使用 Next.js 的 `next/dynamic` 对 `<PDFExportButton />` 组件进行动态引入，配置 `ssr: false` 以避免服务端渲染时因缺失 `window` 对象导致的报错。页面首屏加载时不包含图形渲染库，仅在用户发生点击行为时按需下载执行。

### 4.2 视图与数据解耦的导出方案
* **优化目标:** 解决直接对网页截屏导出 PDF 导致版式不兼容、存在多余 UI 元素的问题。
* **实现逻辑:** 构建一个不可见的 DOM 模板 (`ResumeTemplate`)，将其固定在视口之外并预设 A4 尺寸的固定像素宽度（如 794px）。该组件仅接收脱水后的核心业务数据进行排版。执行导出时，截屏引擎定向对该不可见模板进行渲染生成 PDF，实现前台 UI 与打印输出的逻辑解耦。

### 4.3 边界情况下的 CSS 排版处理
* **处理逻辑:** 针对动态数据可能导致的 Flexbox / Grid 布局塌陷，实施了以下规范：
    * **等高与自适应:** 列表卡片外层统一配置 `h-full`，主体内容区使用 `flex-1` 撑开，保证底部操作栏水平对齐。
    * **空间挤压防护:** 对于固定尺寸的交互元素（如操作按钮），添加 `shrink-0` 属性防止被相邻流式文本挤压变形。
    * **文本溢出控制:** 针对用户输入的不可控连续字符，全局配置
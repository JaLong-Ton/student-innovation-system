# 现代化技术设计文档 (TDD-Next.js栈)

## 敏捷技术选型

| **技术领域**  | **选型**                     | **为什么选它？(Windsurf 友好度)**                            |
| ------------- | ---------------------------- | ------------------------------------------------------------ |
| **核心框架**  | **Next.js (App Router)**     | 原生支持前后端同构，无需单独写后台接口。                     |
| **UI 组件库** | **Tailwind CSS + shadcn/ui** | AI 最懂的 UI 库。直接告诉 AI“给我一个好看的卡片”，它能生成极其惊艳的界面。 |
| **数据库ORM** | **Prisma**                   | 只需要定义好结构，Prisma 自动帮你生成数据库交互代码，AI 写 Prisma Schema 零失误。 |
| **云数据库**  | **Supabase (PostgreSQL)**    | 免费、免安装，提供现成的云端连接字符串。                     |
| **身份认证**  | **Clerk**                    | 几行代码搞定全套登录注册，免去写 token、鉴权的折磨。         |

## 数据库结构设计 (Prisma Schema 预览)

在 Vibe Coding 中，**数据库模型（Schema）是 AI 理解你业务的核心**。只要给 Windsurf 提供以下结构，它就能秒懂并帮你写出所有增删改查代码：

代码段

```
// 1. 用户表 (与 Clerk 联动)
model User {
  id            String         @id @default(cuid())
  email         String         @unique
  name          String?
  achievements  Achievement[]  // 一对多：一个学生有多个成就
  registrations Registration[] // 一对多：一个学生报名多个比赛
  createdAt     DateTime       @default(now())
}

// 2. 个人成就表 (获奖、论文等)
model Achievement {
  id          String   @id @default(cuid())
  userId      String
  user        User     @relation(fields: [userId], references: [id])
  title       String   // 例如："互联网+省级金奖"
  type        String   // AWARD, PAPER, PATENT
  level       String   // STATE, PROVINCE, SCHOOL
  date        DateTime
  createdAt   DateTime @default(now())
}

// 3. 竞赛信息表
model Competition {
  id            String         @id @default(cuid())
  name          String         // 例如："全国大学生数学建模竞赛"
  description   String?
  deadline      DateTime
  registrations Registration[] // 一对多：一个比赛有多人报名
}

// 4. 报名记录表 (关联学生与竞赛)
model Registration {
  id            String      @id @default(cuid())
  userId        String
  user          User        @relation(fields: [userId], references: [id])
  competitionId String
  competition   Competition @relation(fields: [competitionId], references: [id])
  status        String      @default("PENDING") // 状态：待审核、已成功
  createdAt     DateTime    @default(now())
}
```

## 核心架构与数据流 (Next.js Server Actions)

在这个架构下，我们摒弃传统的 API 路由。前端组件提交数据时，直接调用后端的**Server Actions（服务端动作）**。

1. **用户在前端填表：** 比如填写了一个“获得校级一等奖”的表单。
2. **调用 Server Action：** 表单的 `action` 属性直接绑定一个异步函数 `createAchievement(formData)`。
3. **Prisma 入库：** 在这个函数里，利用 Prisma 的 `db.achievement.create()` 直接把数据写入 Supabase。
4. **页面刷新：** 使用 `revalidatePath('/')`，Next.js 会自动刷新页面，最新的奖项立刻显示在列表中。